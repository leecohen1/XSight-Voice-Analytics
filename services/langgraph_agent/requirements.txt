fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2

# Dev/test only
pytest==8.3.3
httpx==0.27.2

# Not yet used (Phase 14) — listed here so the eventual dependency bump is
# a version-pin change, not a new-package surprise. Not installed/imported
# in this phase's code.
# langgraph==0.2.34
