fastapi==0.115.0
uvicorn[standard]==0.30.6
# Bumped from 2.9.2 (Phase 6) to 2.13.4: 2.9.2 has no prebuilt wheel for
# newer CPython runtimes and langgraph 1.2.9 itself requires a compatible
# newer pydantic-core anyway. 2.13.4 stays within pydantic v2's stable API,
# so no model code changes were needed.
pydantic==2.13.4
langgraph==1.2.9

# Dev/test only
pytest==8.3.3
httpx==0.27.2
