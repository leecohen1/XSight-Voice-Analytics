# call_signal_analyser — environment variables
# Phase 6 mock skeleton requires no API keys and downloads no models.
# Variables below are placeholders for the real implementation (Phase 13).

PORT=8002

# Placeholder for Phase 13 (trained PyTorch model weights, not used yet).
MODEL_PATH=./models/call_signal_classifier.pt

# Placeholder for Phase 13 (confidence threshold below which human review is
# required — this value is already used by the Phase 6 mock rules).
HUMAN_REVIEW_CONFIDENCE_THRESHOLD=0.65
