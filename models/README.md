# models

Trained model artifacts for XSight (e.g. the PyTorch call signal classifier weights).

**Status:** Empty. Populated starting Phase 13.

Model weight files are not committed to git (see [.gitignore](../.gitignore)). This folder holds local artifacts only; document how to regenerate them (training script, source data, parameters) rather than committing binaries.
